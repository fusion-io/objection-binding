import {
    connection,
    hasMany,
    hasOne,
    hasOneThrough,
    belongsTo,
    manyToMany,

} from "./decorators";

import Model from "./Model";

export {
    connection,
    hasMany,
    hasOne,
    hasOneThrough,
    belongsTo,
    manyToMany,
    Model
}
